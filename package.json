{
  "name": "ios-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sync": "npx cap sync ios",
    "open": "npx cap open ios",
    "sync:open": "npx cap sync ios && npx cap open ios",
    "build:sync": "cd ../site && bun run publish && cd ../ios-app && npx cap sync ios"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
